{
  "id": "ready_valid_skid_buffer",
  "title": "Ready/Valid Skid Buffer",
  "difficulty": "Hard",
  "category": "Datapath",
  "tags": [
    "ready-valid",
    "buffer",
    "handshake"
  ],
  "statement": "Design a one-entry skid buffer for a ready/valid interface. The buffer accepts input data when in_valid and in_ready are high, and presents data while out_valid is high. It must hold data stable when the downstream side is not ready. Reset clears the valid flag.",
  "module_signature": "module ready_valid_skid_buffer(input clk, input reset, input in_valid, input [7:0] in_data, input out_ready, output in_ready, output reg out_valid, output reg [7:0] out_data);",
  "behavior": [
    "in_ready is high when the buffer is empty or the output is being accepted.",
    "Capture in_data when in_valid and in_ready are high.",
    "Hold out_data stable while out_valid is high and out_ready is low.",
    "Reset clears out_valid.",
    "All state updates occur on the rising edge of clk.",
    "reset is active-high and synchronous unless the problem statement explicitly says otherwise.",
    "Use non-blocking assignments in clocked always blocks.",
    "Outputs must never depend on uninitialized x/z state after reset.",
    "When reset is asserted on a rising edge, registered outputs reset to their documented initial values."
  ],
  "constraints": [
    "Must be synthesizable Verilog.",
    "Use nonblocking assignments in sequential logic.",
    "Do not drop valid data under backpressure."
  ],
  "example": "in_valid out_ready | in_ready out_valid\n1        1         | 1        1\n1        0         | 0        1",
  "hint": "A one-entry valid register is enough for this simplified skid buffer.",
  "template": "module ready_valid_skid_buffer(input clk, input reset, input in_valid, input [7:0] in_data, input out_ready, output in_ready, output reg out_valid, output reg [7:0] out_data);\n  // Write your code here\n\nendmodule",
  "curated": true
}
